1. 静态局部变量（静态空连接） -- 局部变量
	相同点：
		都是局部变量，只能是该函数使用，其他函数无法使用。
	不同点：
		局部变量，每次该函数调用都是重新开始。
		静态局部变量，每次该函数调用都是同一个。类似于，一个该函数的全局变量。

2. 静态局部变量（静态空连接）-- 静态内部变量
	相同点：
		外面的.c文件，都无法使用该变量。只不过是，静态局部变量使用范围更小。只要被使用，就是同一个。
	不同点：
		静态局部变量，是同一个函数多次使用。静态内部变量，是当前文件的多个函数多次使用。

3. 静态内部变量 -- 静态外部变量
	相同点：
		本文件都能使用。是要被使用，它都是同一个。
	不同点：
		静态外部变量，“外部”文件可使用。静态内部变量，只能是本文件“内部”使用。
	
		
